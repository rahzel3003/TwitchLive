interface Config {
    client_id?: string;
    client_secret?: string;
    grant_type?: string;
    scopes?: string;
}
interface TwitchTokenResponse {
    access_token: string;
    expires_in: number;
    token_type: string;
}
export declare function getTwitchAccessToken({ client_id, client_secret, grant_type, scopes, }?: Config): Promise<TwitchTokenResponse>;
export {};
